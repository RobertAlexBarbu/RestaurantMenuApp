import { ActiveFeaturePipe } from './active-feature.pipe'

describe('ActiveFeaturePipe', () => {
    it('create an instance', () => {
        const pipe = new ActiveFeaturePipe()
        expect(pipe).toBeTruthy()
    })
})
