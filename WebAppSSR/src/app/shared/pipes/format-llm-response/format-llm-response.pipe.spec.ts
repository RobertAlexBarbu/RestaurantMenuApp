import { FormatLlmResponsePipe } from './format-llm-response.pipe'

describe('FormatLlmResponsePipe', () => {
    it('create an instance', () => {
        const pipe = new FormatLlmResponsePipe()
        expect(pipe).toBeTruthy()
    })
})
